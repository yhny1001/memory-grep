{
  "display_name": "Memory Grep",
  "loading_order": 60,
  "requires": [],
  "optional": [],
  "js": "entry.js",
  "css": "style.css",
  "author": "yhny1001",
  "version": "0.1.5",
  "description": "Memory compression for TauriTavern. Detects agent runtime via prompt markers (dryRun=false path) and injects a chunked retrieval protocol: snippet-first via chat_search, only fall back to chat_read_messages with small max_chars (2-3k) ranges. Chat-mode keeps pre-grep + sentinel fallback.",
  "homePage": "https://github.com/yhny1001/memory-grep",
  "hooks": {
    "activate": "init"
  }
}
